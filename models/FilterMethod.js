class Filter {
  constructor(recipe) {
    this.recipe = recipe;
    console.log(recipe);
  }

  filterRecipes() {
    const searchInput = document.querySelector("search_input");
    console.log(searchInput);
  }
}
